apiVersion: v1
kind: Service
metadata:
  name: todolist
  namespace: kafka
  labels:
    app: todolist
spec:
  ports:
  - port: 80
    targetPort: 5001
    nodePort: 32501
    protocol: TCP
    name: http
  type: NodePort
  selector:
    app: todolist